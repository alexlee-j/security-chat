## 1. 登录偏好联动

- [x] 1.1 将登录表单改为可随外部状态重置的受控输入，确保用户名、密码、记住密码、自动登录能反映持久化状态
- [x] 1.2 修正 `useChatClient` 的登录偏好写入逻辑，保证自动登录开启时必然勾选记住密码，取消记住密码时自动关闭自动登录
- [x] 1.3 补齐登录恢复流程，让异步加载的历史凭证与偏好状态能正确回填到登录页

## 2. 全局导航统一

- [x] 2.1 抽取或复用统一的全局汉堡菜单触发入口，让会话列表与好友中心共享同一导航抽屉
- [x] 2.2 将好友中心当前独立的抽屉状态迁移到 App 级状态管理，避免两套导航逻辑并存
- [x] 2.3 校准全局导航抽屉中的 profile / settings / about / logout 入口在两个工作区中的行为一致性

## 3. 好友中心重构

- [x] 3.1 按当前桌面总体设计重构好友中心布局，保留左侧列表、右侧详情的双栏结构并统一卡片/间距/圆角风格
- [x] 3.2 将好友中心的联系人详情、搜索、请求处理、黑名单和直聊入口整理为清晰的信息层级
- [x] 3.3 统一好友中心的空态、加载态和窄窗口适配，确保在常见窗口尺寸下仍保持可用

## 4. 会话菜单契约

- [x] 4.1 收敛会话列表右键菜单的可用性判断，隐藏或禁用不支持的动作，并明确区分本地动作与服务端动作
- [x] 4.2 校准删除会话、置顶、静音、复制ID等动作的文案和确认逻辑，避免出现“看得见但不可用”的菜单项

## 5. 验证

- [x] 5.1 补充登录偏好联动与表单回填的单测或组件测试
- [x] 5.2 补充好友中心导航统一与右键菜单的交互 smoke 测试
- [x] 5.3 运行 `pnpm -C apps/desktop build` 并执行桌面端关键路径回归验证
