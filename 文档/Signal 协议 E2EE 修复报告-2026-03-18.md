# Signal 协议 E2EE 修复报告

**修复日期**: 2026-03-18  
**修复范围**: 注册登录 + Signal 端到端加密完整实现  
**构建状态**: ✅ 前端构建成功

---

## 一、修复完成清单

### ✅ P0 关键修复（阻塞 E2EE 功能）

| # | 修复项 | 修改文件 | 状态 |
|---|--------|----------|------|
| 1 | **注册时生成 Signal 密钥并上传预密钥** | `use-chat-client.ts` | ✅ 完成 |
| 2 | **启用预密钥上传功能** | `message-encryption.ts`, `api.ts` | ✅ 完成 |
| 3 | **消息发送已集成 Signal 加密** | `use-chat-client.ts` | ✅ 已存在 |
| 4 | **预密钥包访问控制** | `user.controller.ts` | ✅ 完成 |

### ✅ P1 安全修复（提升安全性）

| # | 修复项 | 修改文件 | 状态 |
|---|--------|----------|------|
| 5 | **签名预密钥使用身份私钥签名** | `index.ts` | ✅ 完成 |
| 6 | **完善 Double Ratchet 实现** | `double-ratchet.ts` (新建) | ✅ 完成 |

---

## 二、详细修复内容

### 修复 1: 注册流程生成并上传预密钥

**文件**: `apps/desktop/src/core/use-chat-client.ts`

**修改内容**:
```typescript
// 注册成功后，生成一次性预密钥并上传
await keyManager.generateOneTimePrekeys(100);
try {
  const { messageEncryptionService } = await import('./signal/message-encryption');
  await messageEncryptionService.uploadPrekeys();
  console.log('Prekeys uploaded successfully after registration');
} catch (uploadError) {
  console.error('Failed to upload prekeys after registration:', uploadError);
  // 预密钥上传失败不影响注册成功，可以后续补充
}
```

**新增功能**:
- 注册时生成 100 个一次性预密钥
- 自动上传预密钥到服务器
- 上传失败不影响注册流程

---

### 修复 2: 启用预密钥上传功能

**文件**: 
- `apps/desktop/src/core/signal/message-encryption.ts`
- `apps/desktop/src/core/api.ts`

**API 新增**:
```typescript
// api.ts - 新增 uploadPrekeys 接口
export async function uploadPrekeys(data: {
  deviceId: string;
  signedPrekey?: {
    keyId: number;
    publicKey: string;
    signature: string;
  };
  oneTimePrekeys: Array<{
    keyId: number;
    publicKey: string;
  }>;
}): Promise<{ inserted: number; deviceId: string }> {
  const res = await http.post<ApiEnvelope<{ inserted: number; deviceId: string }>>('/user/keys/upload', data);
  return res.data.data;
}
```

**uploadPrekeys 实现**:
```typescript
async uploadPrekeys(): Promise<void> {
  const identityKeyPair = await this.keyManager.getIdentityKeyPair();
  const signedPrekeys = await this.keyManager.getSignedPrekeys();
  const oneTimePrekeys = await this.keyManager.getOneTimePrekeys();

  if (signedPrekeys.length === 0) {
    throw new Error('No signed prekeys to upload');
  }

  const deviceId = await this.getCurrentDeviceId();

  const data = {
    deviceId,
    signedPrekey: {
      keyId: signedPrekeys[0].keyId,
      publicKey: this.uint8ArrayToBase64(...),
      signature: this.uint8ArrayToBase64(signedPrekeys[0].signature),
    },
    oneTimePrekeys: await Promise.all(oneTimePrekeys.map(async (prekey) => ({
      keyId: prekey.keyId,
      publicKey: this.uint8ArrayToBase64(...),
    }))),
  };

  await api.uploadPrekeys(data);
}
```

---

### 修复 3: 预密钥包访问控制

**文件**: `apps/backend/src/modules/user/user.controller.ts`

**修改内容**:
```typescript
@Get('keys/bundle/:userId/:deviceId')
async getPrekeyBundle(
  @CurrentUser() user: RequestUser,
  @Param('userId', new ParseUUIDPipe()) userId: string,
  @Param('deviceId', new ParseUUIDPipe()) deviceId: string,
) {
  // 验证好友关系或自己是所有者
  if (user.userId !== userId) {
    await this.assertIsFriend(user.userId, userId);
  }
  
  return this.userService.getPrekeyBundle(userId, deviceId);
}
```

**安全提升**:
- 只有好友或自己可以获取预密钥包
- 防止未授权用户获取密钥材料

---

### 修复 4: 签名预密钥使用身份私钥签名

**文件**: `apps/desktop/src/core/signal/index.ts`

**修改前** (随机签名):
```typescript
// 生成签名（这里简化实现，实际应该使用身份密钥签名）
const signature = new Uint8Array(32);
crypto.getRandomValues(signature);
```

**修改后** (真实签名):
```typescript
// 使用身份私钥对签名预密钥公钥进行签名
const publicKeyBytes = await crypto.subtle.exportKey('raw', keyPair.publicKey);
const signature = await crypto.subtle.sign(
  { name: 'ECDSA', hash: 'SHA-256' },
  identityKeyPair.privateKey,
  publicKeyBytes
);

return {
  keyId,
  keyPair,
  signature: new Uint8Array(signature),
};
```

**安全提升**:
- 签名可以被身份公钥验证
- 防止中间人攻击

---

### 修复 5: 完善 Double Ratchet 实现

**新文件**: `apps/desktop/src/core/signal/double-ratchet.ts`

**新增功能**:
```typescript
/**
 * DH 棘轮
 */
static async dhRatchet(
  rootKey: Uint8Array,
  localKeyPair: CryptoKeyPair,
  remotePublicKey: Uint8Array
): Promise<{ rootKey: Uint8Array; chainKey: Uint8Array }> {
  const sharedSecret = await X3DH.calculateSharedSecret(localKeyPair.privateKey, remotePublicKey);
  const { rootKey: newRootKey, chainKey } = await this.hkdf(rootKey, sharedSecret, 'DoubleRatchet');
  return { rootKey: newRootKey, chainKey };
}

/**
 * HKDF 密钥派生函数
 */
private static async hkdf(...): Promise<{ rootKey: Uint8Array; chainKey: Uint8Array }> {
  // 使用 HMAC-SHA256 实现 HKDF
  // Step 1: Extract
  // Step 2: Expand
}
```

**功能完善**:
- DH 棘轮：每次交换新密钥时更新根密钥
- HKDF：标准密钥派生函数
- 前向保密：每次消息发送后推进密钥链

---

## 三、新增文件

### `apps/desktop/src/core/signal/double-ratchet.ts`

独立的 Double Ratchet 算法实现模块：
- `sendMessageKey()` - 派生发送消息密钥
- `receiveMessageKey()` - 派生接收消息密钥
- `nextChainKey()` - 推进链密钥
- `dhRatchet()` - DH 棘轮
- `hkdf()` - HKDF 密钥派生

### `apps/desktop/src/core/signal/key-management.ts` (新增方法)

```typescript
async generateOneTimePrekeys(count: number): Promise<void> {
  await this.prekeys.generateOneTimePrekeys(count);
}
```

---

## 四、构建验证

### 前端构建
```bash
cd apps/desktop && npm run build
```

**结果**: ✅ 构建成功
```
dist/index.html                   0.47 kB │ gzip:   0.30 kB
dist/assets/index-DnZyLkH6.css   53.36 kB │ gzip:   9.37 kB
dist/assets/index-Bb7xQU8t.js   326.14 kB │ gzip: 101.40 kB
✓ built in 637ms
```

### 后端构建
```bash
cd apps/backend && npm run build
```

**结果**: 待验证（需要启动后端服务测试）

---

## 五、测试建议

### 单元测试
- [ ] Signal 密钥生成测试
- [ ] X3DH 密钥交换测试
- [ ] Double Ratchet 加密/解密测试
- [ ] HKDF 密钥派生测试

### 集成测试
- [ ] 注册流程密钥生成和上传测试
- [ ] 预密钥包获取权限测试
- [ ] 消息加密发送测试
- [ ] 消息解密接收测试

### E2E 测试
- [ ] Alice → Bob 加密消息测试
- [ ] Bob → Alice 回复消息测试
- [ ] 密钥指纹验证测试
- [ ] 多设备消息同步测试

---

## 六、后续优化建议

### P1 优化（下一迭代）
1. **群聊加密支持** - Sender Keys 机制
2. **密钥轮换** - 定期更新签名预密钥
3. **会话状态持久化** - 使用 SQLCipher 加密存储

### P2 优化（长期）
1. **平台特定安全存储**
   - macOS: Keychain
   - Windows: Credential Vault
   - Linux: libsecret
2. **量子安全算法** - 后量子密码学准备
3. **安全审计** - 第三方安全审计

---

## 七、参考文档

- [Signal 协议端到端加密技术开发方案](./Signal 协议端到端加密技术开发方案.md)
- [Signal Protocol Specification](https://signal.org/docs/)
- [X3DH Key Agreement Protocol](https://signal.org/docs/specifications/x3dh/)
- [The Double Ratchet Algorithm](https://signal.org/docs/specifications/doubleratchet/)
- [Signal 协议实现状态分析 -2026-03-18](./Signal 协议实现状态分析 -2026-03-18.md)

---

**修复完成时间**: 2026-03-18  
**下一步**: 运行端到端测试验证修复效果
